# Events having prefix 'syslog.authpriv.info.' will be stored both on files.
#<match syslog.authpriv.info>
#  @type file
#  path /fluentd/syslog
#</match>


# All other events will be printed to stdout
<match **>
  @type stdout
</match>