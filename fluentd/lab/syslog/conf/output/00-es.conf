# Events having prefix 'system.' will be stored both on files.
<match syslog.authpriv.info>
  @type copy
  <store>
    @type file
    path /fluentd/syslog
    flush_interval 30s
  </store>
</match>

#All other events will be printed to stdout. Emxample system.authpriv.debug
<match **>
  @type stdout
</match>